# <service>

One-paragraph purpose + the concept cluster this service exists to exercise.

## Endpoints / protocols

## Config (env vars)

## Design doc & ADRs
`docs/<service>/design.md` · `docs/<service>/adr/`

## Demo (≤5 commands)
```bash
make up PROFILE=core
make build SVC=<service>
# run, hit, observe
```
Expected observable result (dashboard panel / trace / curl output).
