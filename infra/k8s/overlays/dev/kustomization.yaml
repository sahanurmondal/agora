apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: agora
resources:
  - ../../base
# dev = as-is (replicas 2 for the pod-kill demo). A demo/ overlay can patch
# replicas/resources up when the full-cluster demos run.
